Find the skill to create new agent (create_new_agent.md), and: **Create a new agent named Mongoxer that connects to a mongo database and let the user specify the mongo connection settings, and the script in its configuration, in order to this script configured  to be executed and once the script is executed the result must be placed in the new agent's log file, and start up the agents connected to its output**.