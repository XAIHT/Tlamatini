# Your real credentials — NEVER commit these. Ship secrets.yaml.example instead.
secrets.yaml

# ESPHome's build tree: the vendored PlatformIO project, toolchains and the
# compiled firmware. Large, machine-specific, and fully regenerated by a compile.
.esphome/
*.bin
*.elf
